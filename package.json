{
  "name": "nodecached",
  "version": "1.0.0",
  "description": "Cache files locally instead of fetching on the remote server every time",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "TheStaticTurtle",
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "axios": "^0.19.2",
    "express": "^4.17.1",
    "get-folder-size": "^2.0.1",
    "md5": "^2.2.1",
    "request": "^2.88.2"
  }
}
